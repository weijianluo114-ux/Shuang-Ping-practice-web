/* 版本与发布渠道：
   channel = "release" 正式发行版（main 分支，GitHub Pages 发布）
   channel = "dev"     本地测试版（dev 分支，本地 file:// 打开）
   每次正式发布时请同时更新 version。 */
window.APP_VERSION = { version: "1.1.0", channel: "release" };
