# 双拼练习 · 项目开发文档（dev 专用）

> ⚠️ **本文件只应存在于 `dev` 测试分支**，正式发布到 `main` 时不要带过去。
> 行号以本文件创建时为准：`app.js` 990 行、`article.js` 1049 行、`styles.css` 414 行、`index.html` 298 行。

---

## 1. 项目概览

- 纯本地、无构建、无服务器、无框架的双拼打字练习网页（仿 shuangpin.xyz 核心功能）。
- 技术栈：原生 HTML + CSS + 原生 JS（ES6），唯一第三方库 `pinyin-pro.min.js` v3.29.3（汉字转拼音，MIT License）。
- 运行方式：双击 `index.html`（file:// 协议）；正式版发布到 GitHub Pages。
- 数据：全部存浏览器 `localStorage`，无任何上传。file:// 与 GitHub Pages 是不同 origin，数据天然隔离。

## 2. 运行与发布架构（双分支）

| 分支 | 角色 | 发布 |
| --- | --- | --- |
| `main` | 正式发行版 | GitHub Pages：`https://weijianluo114-ux.github.io/Shuang-Ping-practice-web/` |
| `dev`  | 本地测试版 | 不发布，本地 `file://` 打开测试 |

- 版本号在 `version.js`（5 行）：`window.APP_VERSION = { version, channel }`。
  - `channel:"release"` → 正式版（main），顶栏绿色徽标 `v1.0.0`；
  - `channel:"dev"` → 测试版（dev），顶栏红色徽标 `测试版 v1.0.0`。
  - 徽标渲染函数：`app.js:859 renderVersionBadge()`，启动时调用（`app.js:985`）。
- 日常流程：在 dev 改 → 本地双击测试 → `node --check` 语法检查 → 合并到 main（见 §15）→ push → Pages 约 1 分钟更新 → 打 tag `vX.Y.Z`。

## 3. 文件清单

| 文件 | 行数 | 职责 | 修改注意 |
| --- | --- | --- | --- |
| `index.html` | 298 | 页面结构：5 个视图 + 设置弹窗 + 脚本标签 | 改 id/结构时要同步 CSS 与 JS 选择器 |
| `styles.css` | 414 | 全部样式：主题变量、五个视图、文章模块 | 文章样式集中在 314 行之后 |
| `app.js` | 990 | 核心引擎：方案数据、编码转换、键位图/记忆/综合练习/统计/设置、全局 `$`/`$$` | 顶层无 IIFE，全局词法作用域；改动会影响 article.js |
| `article.js` | 1049 | 文章练习 + 好词好句模块（IIFE） | 依赖 app.js 的 `$`、`$$`、`settings`、`codeFor`、`mergeRoundIntoStats` 等全局 |
| `articles.js` | 2（单行 JSON） | `window.SP_ARTICLES`：内置文章与句子数据池 | 结构见 §8 |
| `pinyin-pro.min.js` | 1（压缩） | `window.pinyinPro`，汉字→拼音 | 不要改动 |
| `version.js` | 5 | 版本号与渠道 | 正式发布时改回 release |
| `README.md` | 82 | 用户向说明 | — |
| `.gitignore` | 1 | 忽略 .DS_Store/Thumbs.db/desktop.ini/*.log | — |

## 4. 脚本加载顺序与全局契约

`index.html:291-295` 的加载顺序（**顺序不能乱**）：

```
pinyin-pro.min.js  →  articles.js  →  version.js  →  app.js  →  article.js
```

跨文件全局契约（article.js 依赖这些全局）：

| 全局 | 来源 | 说明 |
| --- | --- | --- |
| `window.pinyinPro` | pinyin-pro.min.js | `pinyin(text, {toneType:"none", type:"array", v:true})` 返回字符串数组 |
| `window.SP_ARTICLES` | articles.js | 文章数据池 |
| `window.APP_VERSION` | version.js | 版本与渠道 |
| `$` / `$$` | app.js:245-246 | `document.querySelector` / `querySelectorAll` 简写，article.js 直接复用 |
| `SCHEMES` / `currentScheme` | app.js:3 / app.js:231 | 四套方案数据与当前方案 |
| `settings` | app.js:232 | 全局设置对象，article.js 直接读写 |
| `LS_KEYS` | app.js:216 | localStorage 键名常量 |
| `codeFor(py)` | app.js:288 | 拼音 → 当前方案双拼编码（两键或零声母） |
| `mergeRoundIntoStats(rs, elapsedMs)` | app.js:757 | 把文章会话统计合入全局统计 |
| `applyArticleFont()` | app.js:841 | 应用字体/粗体 CSS 变量，article.js 调用 |
| `renderArtKeyboard()` / `flashArtKey(key)` | app.js:465 / app.js:489 | 文章页迷你键盘渲染与按键高亮 |
| `currentView` | app.js:233 | 当前视图名 |
| `setView(name)` | app.js:345 | 切视图（会调 `ArticlePractice.onView`） |
| `escapeHtml` / `fmtTime` / `todayStr` | app.js:247 / 312 / 317 | 工具函数，article.js 复用 |
| `window.ArticlePractice` | article.js 末尾 | `{ onView, onKey, onEscape, onSchemeChange, onSettings, renderList }` |

## 5. localStorage 数据字典

`app.js:216` 定义全局键；`article.js:16` 定义文章模块键。

| 键 | 内容 | 写/读 |
| --- | --- | --- |
| `sp_scheme_v1` | 方案 id（`xiaohe`/`ziranma`/`abc`/`microsoft`，默认 xiaohe） | app.js |
| `sp_settings_v1` | `{hint,sound,theme,fontSize,articleFont,articleBold}` | app.js；article.js 只读写 hint/articleFont/articleBold |
| `sp_stats_v1` | `{totalCorrect,totalWrong,totalItems,totalTimeMs,bestCombo,byKey,days:{...}}` | app.js `mergeRoundIntoStats` |
| `sp_custom_v1` | 自定义拼音串（空格分隔） | app.js |
| `sp_view_v1` | 上次退出时的视图名 | app.js `setView` |
| `sp_article_progress_v1` | `{id, idx, ts}` 普通文章断点（idx 为 flat 下标） | article.js |
| `sp_article_done_v1` | `{id:{n,last}}` 已练文章计数 | article.js `markDone` |
| `sp_good_v1` | `{total,timeMs,days:{},progress:{pool,idx,ts}}` 好词好句累计 | article.js |
| `sp_article_mode_v1` | `"good"` 或 `"article"`，文章页子模式记忆 | article.js `saveArticleMode` |

- `lsGet(key, fallback)` / `lsSet(key, value)`：app.js:218-228，JSON 序列化 + try/catch 容错。
- 设置弹窗「清除」只清 `sp_stats_v1`。

## 6. app.js 详解

### 6.1 双拼方案数据（app.js:3-81）

`SCHEMES` 数组，4 个方案对象，每个包含：

- `id` / `name` / `engName` / `color` / `desc` / `features`
- `zeroInitialRule`：`"double_pinyin"`（小鹤/自然码，零声母双写）或 `"o_prefix"`（智能ABC/微软，o 前缀）
- `ruleText`：零声母规则说明
- `initials`：`{zh:"v", ch:"i", sh:"u"}` 声母键
- `finals`：韵母→键 映射（含 `ü/v` 变体键）
- `displayMap`：键盘键→承担韵母说明文本（如 `q:"iu"`）

**加新方案**：复制一个对象，改 id/name/finals/displayMap/ruleText，方案选择器与键盘图会自动生成。

### 6.2 数据常量（app.js:82-214）

- `PINYIN_INITIALS`（23 个，含 zh/ch/sh）、`DRILL_INITIALS`（23 个，排序不同）、`ZERO_FINALS`（12 个）
- `SYLLABLE_TEXT` → `SYLLABLES`（空格分隔音节表，app.js:87-118）
- `WORD_LINES`（app.js:120）→ `WORDS = WORD_LINES.map(lineToRecord)`，记录格式 `{text, pys}`（空格分隔拼音，`:n` 标记可选项）
- `SENTENCE_LINES`（app.js:191）→ `SENTENCES`

### 6.3 编码链（核心算法，app.js:260-290）

```
parseSyllable(py) → {initial, final}
normalizeFinal(initial, final) → 规范化韵母
toCode(initial, rawFinal, scheme) → 双拼两键
codeFor(py) → 当前方案编码
```

- `parseSyllable`（app.js:260）：从 `PINYIN_INITIALS` 最长前缀匹配声母；匹配不到为零声母（`initial=""`）。
- `normalizeFinal`（app.js:267）：把 `v`→`ü`；处理 j/q/x 后 `ü` 的省写（`ju→j+ü`、`juan→j+üan` 等）；零声母直接返回原韵母。
- `toCode`（app.js:272）：
  - 有声母：`scheme.initials[initial]`（zh/ch/sh 有映射，其余取首字母）+ `scheme.finals[final]`；
  - 零声母 + `double_pinyin`：两字母韵母按全拼；单字母韵母双写；ang/eng/ing/ong 取首字母+韵母键；
  - 零声母 + `o_prefix`：`o` + 韵母键（o 本身除外）。
- `isPunct(ch)`（app.js:292）与 `buildSentenceItems(sentence)`（app.js:295）：句子练习时把标点做成 skip 字符（空格跳过），汉字转 code。

### 6.4 全局状态（app.js:230-242）

- `schemeId` / `currentScheme` / `settings` / `currentView`
- 综合练习：`roundItems, roundIdx, roundTyped, roundStats, roundActive, finished, startedOnce, flashWrongItem, practiceMode`
- 计时：`timerBase, timerStart, timerInterval`
- 键位记忆：`drill = {kind, items, idx, typed, correct, wrong, wrongKeys, total, active, finished, lock}`

### 6.5 函数地图（app.js）

| 区块 | 函数（行号） |
| --- | --- |
| 工具 | `$` 245、`$$` 246、`escapeHtml` 247、`shuffle` 250、`pick` 258、`fmtTime` 312、`todayStr` 317 |
| 音效 | `beep` 323、`soundCorrect` 338、`soundWrong` 339、`soundDone` 340 |
| 视图 | `setView` 345、`handleTimerOnViewChange` 357 |
| 计时 | `getElapsed` 363、`startTimer` 366、`pauseTimer` 375、`resumeTimer` 379、`resetTimer` 388 |
| 键位图 | `renderSchemePicker` 393、`applySchemeColor` 399、`renderSchemeHero` 403、`renderSchemeRule` 413、`renderKeyboard` 424、`bindKeyClicks` 446、`showKeyDetail` 451、`flashKey` 459、`renderArtKeyboard` 465、`bindArtKeyClicks` 486、`flashArtKey` 489 |
| 键位记忆 | `startDrill` 497、`renderDrill` 515、`handleDrillKey` 545 |
| 综合练习 | `switchPracticeMode` 578、`startPracticeRound` 586、`newPracticeRound` 597、`startCustomRound` 618、`resetPracticeToSetup` 632、`renderPractice` 640、`renderLiveStats` 669、`handlePracticeKey` 686、`finishRound` 728 |
| 统计 | `loadStats` 752、`mergeRoundIntoStats` 757、`renderStats` 773 |
| 设置/主题 | `applyTheme` 815、`ARTICLE_FONTS` 822、`applyArticleFont` 841、`FONT_SIZES` 845、`FONT_SIZE_PX` 846、`normalizeFontSize` 847、`applyFontSize` 852、`updateTopbarH` 855、`renderVersionBadge` 859、`openModal` 867、`closeModal` 876 |
| 事件/启动 | `bindEvents` 893、启动序列 979-990 |

### 6.6 启动序列（app.js:979-990）

```js
applyTheme(settings.theme);
applyFontSize();
applyArticleFont();
applySchemeColor();
renderSchemePicker();
updateTopbarH();
renderArtKeyboard();
renderVersionBadge();
setView(lsGet(LS_KEYS.view, "article"));   // 首次默认 article，之后恢复上次视图
bindEvents();
window.addEventListener("resize", updateTopbarH);
```

## 7. article.js 详解

### 7.1 结构

- 整体是 IIFE：`(function(){ ... })();`（article.js:8 开始，1049 结束）。
- 末尾导出：`window.ArticlePractice = { onView, onKey, onEscape, onSchemeChange, onSettings, renderList }`（article.js:1046）。
- 末尾启动补调：`if (currentView === "article") onView("article");`（article.js:1048）——实现「默认/恢复文章页时渲染列表并自动开文」；其它视图由 app.js 的 setView 完成初始化。

### 7.2 局部常量与状态（article.js:15-48）

- `HAN` 正则：匹配中日韩统一表意文字（article.js:15）。
- `A_LS`（article.js:16）：`progress / done / good / mode` 四个键。
- 状态对象 `A`（article.js:18-48）关键字段：
  - `session`：`{title, byline, groups:[[char...]], flat:[char...]}`，**groups 每个元素是一段（`<p class="apara">`），flat 是全部汉字拍平**；
  - `curIdx`：当前待打汉字在 flat 中的下标；`total` 待打总数；
  - `baseDone` / `sessionStartIdx`：断点续练时本次会话前已完成字数 / 最早可回退下标；
  - `stats`：本次会话 `{correct,wrong,itemsDone,bestCombo,combo,wrongKeys}`；
  - `goodMode/good/goodLoading/goodPrefetching/goodBatchGen/goodWinStart/goodRemovedChars/goodDoneBase/instantScroll`：好词好句模式专用（见 7.7）；
  - `speeds/speedWrong/lastCharTime`：速度曲线采样与错字标记（见 7.6）；
  - `timerBase/timerStart/timerIv`：文章计时。

### 7.3 汉字→双拼编码流程

- `singlePy(ch)`（article.js:84）：单字调 `window.pinyinPro.pinyin(ch,{toneType:"none",type:"array",v:true})`，取第一读音，失败返回 `"?"`。
- `tokenizeParagraphs(paragraphs)`（article.js:91-111）：每段 `Array.from(text.trim())` 拆字；整段统一调一次 `pinyinPro.pinyin(段落)` 得到数组（多音字消歧）；每字对象字段：
  - `ch`（汉字）、`py`（拼音）、`code`（`codeFor(py)` 双拼编码）、`skip`（标点/空白跳过）、`g`（段索引，buildSession 里赋值）、`done/typedLen/errs/wrongRun/revealed/el/pyEl`。
- `codeFor` 来自 app.js（全局）。

### 7.4 建会话与断点续练

- `buildSession(title, byline, paragraphs)`（article.js:114）：构建 session、给每字赋 `g`、初始化状态、同步 `#art-hint/#art-font/#art-bold` 与 `applyArticleFont`、渲染标题/正文/统计/速度图。
- `applyResume(idx)`（article.js:148）：把 `flat[0..idx-1]` 的汉字标为 done（`typedLen=code.length`），设 `baseDone`、`sessionStartIdx=idx`——用于「继续上次位置」。
- 普通文章断点保存：`saveProgress()`（article.js:600）写 `sp_article_progress_v1 = {id, idx, ts}`。

### 7.5 渲染模型（关键）

- `cellHtml(c)`（article.js:172）：
  ```html
  <span class="ac [done] [reveal]" [data-errs="1..4"]>
    <i class="py"><em>已敲对的码</em></i>
    <b class="hz">字</b>
  </span>
  ```
  - `.ac` 字格（inline-flex，拼音在上、汉字在下）；`.py` 拼音槽固定宽 2.5rem 防回流；`.hz` 汉字。
- `renderText()`（article.js:179）：**整段全文渲染**（不再分段显示），`groups.map(g => <p class="apara">...</p>)`，然后 `box.querySelectorAll(".ac")` 回填每字的 `c.el` 与 `c.pyEl` 引用。
- `setCur(j)`（article.js:194）：移除旧 `.cur`，给新当前字加 `.cur`，调 `renderCurSlots()` 与 `positionArtText()`。
- `renderCurSlots()`（article.js:247）：把当前字 `.py` 的已敲部分渲染成 `<em class="hit">`，未敲部分按 `settings.hint` 决定显示或隐藏。
- `refreshHints()`（article.js:265）：拼音提示开关切换时，逐字按 `settings.hint` / `revealed` 刷新 `.py` 文本。
- `revealHintFor(c)` / `clearReveal(c)`（article.js:285/291）：无提示模式下连错 3 次临时显拼音（见 7.8）。

### 7.6 打字 / 回退 / 速度曲线

- `handleType(k)`（article.js:432）：`k` 为实体键（`[a-zA-Z;]` 才处理）。当前字 `code[typedLen]` 与 `k` 比较：
  - 正确：`typedLen++`、`c.wrongRun=0`、`renderCurSlots()`；`typedLen===code.length` 时该字完成——记录 `hadErrs`，`recordSpeed(hadErrs)`，`nextChar()`；
  - 错误：`A.stats.wrong++`、`flashErr()`（累计 `c.errs`、设 `data-errs`、连错显拼音逻辑）。
- `nextChar()`（article.js:466）：当前字加 `.done`、找下一个非 skip；普通文章完成则 `finish()`；好词好句走滑动窗口逻辑（见 7.7）。
- `stepBack()`（article.js:511）：当前字已敲键则退一键；否则回上一字（最多回到 `sessionStartIdx`），同步 `speeds.pop()`。
- `finish()`（article.js:549）：`mergeRoundIntoStats`、`markDone(articleId)`、清进度、填 `#art-finish-grid`、显示 `#art-finish`。
- 速度采样 `recordSpeed(wrong)`（article.js:413）：每完成一个字记一个瞬时速度（字/分），首字用 `1/(elapsed/60000)`，之后用 `60000/dt`，上限 400；`wrong` 为真时 `A.speedWrong.push(true)`。
- `updateSpeedChart()`（article.js:376）：SVG 面积图 + Catmull-Rom 平滑曲线（`smoothSpeedPath` 345 行 / `speedCubicSegments` 361 行）；**错字分段用红色 `var(--bad)`，正确分段用绿色 `var(--accent)`**；纵轴上限 `min(rawMax,200)`，只画最近 60 个采样点。

### 7.7 好词好句模式（滑动窗口 + 无限续练）

- 入口：文章列表里的 `#btn-art-good` 卡片（`goodCardHtml` 650 行渲染，显示已练 X 句/时长/坚持天数/可续练）。
- `startGood()`（article.js:778 附近）：
  - `saveArticleMode("good")`；
  - 记录当天 `days[todayStr()]=1`；
  - 有 `sp_good_v1.progress.pool` 则 `goodBatchGen+1`、恢复 pool、`buildSession` + `applyResume` + 滑动窗口；否则 `goodNextBatch()` 取首批 10 句。
- `fetchSentences(n)`（article.js:893 附近）：`fetch("https://v1.hitokoto.cn/?c=a&c=b&c=d&c=i&c=k&min_length=8&max_length=30")` 抓 10 句（失败回退内置句库）；`startOnline` 用于「联网十句」卡片。
- `goodNextBatch()`（async）：初始批（重置窗口计数后 `buildSession`）。
- `goodPrefetch()`（async）：第 7 句起预加载下一批（`goodRecordSentence(g)` 中 `if(g>=6) goodPrefetch()`）；防重：`goodBatchGen` 代号 + pool 长度不变 + 至少 3 条才追加。
- `goodAppendSentences(list, extendPool)`（article.js:657）：新句子 tokenize 后 `insertAdjacentHTML` 追加到 `#art-text` 底部，同步 `groups/flat` 与 `c.el/pyEl` 映射，`A.total += 新增字数`。
- `goodRemoveOldBefore(newG)`（article.js:686）：**只保留当前句往前 2 句**，删除更早的 `.apara`，同步维护 `goodWinStart/goodRemovedChars/goodDoneBase/A.total/sessionStartIdx`——窗口内 DOM 有界（实测 .apara 约 6~14 个）。
- `goodSlideWindow(j)`：`j - goodRemoveOldBefore(s.flat[j].g)`，把窗口内下标换算为删除后的 DOM 下标。
- 进度换算：`updateLive()`（article.js:318）好词好句分支 `done = goodDoneBase + 窗口内done`、`total = goodDoneBase + A.total`，避免删旧句后进度回退。
- 续练保存：`saveProgress()` 好词好句分支写 `g.progress = {pool: pool.slice(), idx: goodRemovedChars + curIdx, ts}`。
- 退出 `exitToList()`（article.js:580）：`goodFlushTime()`（累计时长）+ `saveProgress()` + 清 `goodMode/goodNextPool 相关状态`。
- `beforeunload`（article.js:1040）：好词好句未完成时兜底保存。

### 7.8 无提示连错 3 次显拼音

- `flashErr()`（article.js:301）：`c.wrongRun++`；当 `!settings.hint && c.wrongRun>=3 && !c.revealed` 时 `revealHintFor(c)`（显示完整拼音 + 加 `.reveal` 类，橙色）。
- 打对完成该字时 `clearReveal(c)` 隐藏；退格也清除。

### 7.9 固定两行窗口与滚动（重要）

- CSS：`.art-text` 固定高度 = 2 行 + padding，`overflow:hidden`（styles.css:359-365）。
- `positionArtText()`（article.js:223）：把**当前字所在行滚动到窗口内容区垂直居中**：
  1. `contentCenter = padTop + (clientHeight - padTop - padBottom)/2`；
  2. `lineTop = elRect.top - boxRect.top + box.scrollTop`（换算内容坐标）；
  3. `targetY = lineTop + lineH/2 - contentCenter`，钳制在 `[0, scrollHeight-clientHeight]`；
  4. `smoothScrollTo(box, y)`。
- `smoothScrollTo(box, y)`（article.js:206）：rAF + easeOutCubic，`dur = min(260, max(140, |dist|*0.45))`，`id = ++scrollAnimId` 作废旧动画。
- `instantScroll` 分支：`box.scrollTop = y` 直接定位（保留字段，当前不置 true）。
- 触发点：`setCur`、`onSettings`、`#art-font/#art-bold` change、window resize。

### 7.10 事件绑定（article.js:1012 附近）

- `#article-list` click：按 `data-art` id 开文；`btn-art-good` 开好词好句；`btn-art-online` 联网十句（fetch 失败回退内置）。
- `#btn-art-exit` / `#btn-art-list-side` / `#btn-art-list` / `#art-finish` 背景点击 → `exitToList`。
- `#btn-art-restart` / `#btn-art-again` → `restartLast`（再练一次，好词好句先 flush 时长）。
- `#btn-art-custom` → `startCustom`（textarea 文本，空行分段）。
- `#art-hint` change → `settings.hint` + `refreshHints`；`#art-font` / `#art-bold` change → 写 settings + `applyArticleFont` + `positionArtText`。

## 8. articles.js 数据结构

单行：`window.SP_ARTICLES = { ... }`。

```js
{
  "classics": [ { "id":"c-chun", "title":"春（节选）", "author":"朱自清",
                  "paragraphs":["……","……"] }, ... ],   // 7 篇公版经典
  "originals": [ { "id":"o-xxx", "title":"...", "author":"...",
                  "paragraphs":[...] }, ... ],            // 4 篇原创
  "sentences": [ "……", ... ]                              // 53 句（好词好句回退池、随机十句池）
}
```

- `findArticle(id)`（article.js:616）在 `classics + originals` 里查找。
- 新增文章：往 `classics` 或 `originals` 加对象，id 唯一；文章列表与断点续练自动生效。注意 paragraphs 的汉字必须能被 pinyin-pro 转拼音，否则 `singlePy` 回退 `"?"`。

## 9. styles.css 详解

### 9.1 主题变量（styles.css:1-28）

`:root` 与 `[data-theme="dark"]` 定义：`--accent/--accent-soft/--bg/--paper/--card/--text/--muted/--line/--ok/--bad/--bad-soft/--shadow/--radius`。
- 护眼纸色：浅色 `--paper:#F7F2E7`，深色 `--paper:#1C221A`；文章正文区强制用纸色（深色下也有专门覆盖）。
- 主题由 `applyTheme(theme)`（app.js:815）设置 `<html data-theme>`，`theme` 取 `auto/light/dark`。

### 9.2 主要区块

| 行号 | 区块 |
| --- | --- |
| 37-97 | 顶栏 + 版本徽标 + 左侧竖向导航 `.tabs`（fixed 在品牌区下方） |
| 117-157 | 键位图 |
| 158-179 | 键位记忆 |
| 180-255 | 综合练习 + `.live-stats` chips |
| 256-272 | 统计 |
| 273-313 | 设置弹窗 + @media 720 响应式 |
| 314-413 | **文章练习全部样式**（见下） |
| 402-414 | @media 1080 折叠单列 |

### 9.3 文章模块关键样式

- `.art-layout`（337）：三栏 grid `minmax(150px,210px) minmax(0,1fr) minmax(170px,210px)`，gap 14px。
- `.art-side`（338-339）：`position:sticky; top:78px`，左栏控件卡、右栏操作按钮；右栏 `.live-stats` 竖排右对齐（350-351）。
- `.art-center`（348）：`max-width:820px; margin:0 auto; margin-top:-6px`（整体上移一点点）。
- `.art-text`（359-365）：**固定两行窗口** `height:calc(2*2.35em + 44px); overflow:hidden; position:relative`，纸色背景。
- `.ac` 字格（367-383）：
  - `.ac`：inline-flex、`.py` 固定宽 2.5rem 防回流；
  - `.ac.cur .hz{color:#111}`（当前字黑色，浅绿背景+描边）；
  - `.ac.done .hz{color:var(--ok)}`（正确绿色）；
  - `.ac[data-errs="1".."4"] .hz`（错误红：`#EF5350/#E53935/#C62828/#8B0000` 渐深）；
  - `.ac.skip`（标点灰）、`.ac.err`（抖动动画 artShake）。
- `.art-text.nohint .py{visibility:hidden}` + `.ac.reveal .py` 橙色显拼音（386-387）。
- `.art-speed`（354）：速度曲线容器 `height:120px`。
- `.art-bottom`（390）：`margin-top:auto` 把迷你键盘压到页面底部；`.art-kb`（392）`width:fit-content` 只包键盘。
- `.art-card.done`（326）+ `.badge`（325）：已练文章高亮与「✓ 已练」徽标。

## 10. index.html DOM 地图（供 JS 选择器对照）

| 区域 | 关键 id |
| --- | --- |
| 顶栏 | `topbar`、`ver-badge`、`scheme-picker`、`btn-settings` |
| 导航 | `.tabs` 内 5 个 `.tab[data-view]`：keyboard/drills/practice/article/stats |
| 键位图 | `view-keyboard`、`scheme-hero`、`keyboard`、`key-detail`、`scheme-rule` |
| 键位记忆 | `view-drills`、`drill-modes`、`drill-progress-text`、`drill-bar`、`drill-kind`、`drill-target-label`、`drill-code-slots`、`drill-feedback`、`btn-drill-reset` |
| 综合练习 | `view-practice`、`practice-setup`、`practice-modes`、`setup-row-syll`、`round-size`、`word-size`、`btn-start-round`、`setup-row-custom`、`custom-input`、`btn-start-custom`、`practice-area`、`live-stats`、`btn-practice-reset`、`progress-fill`、`stage`、`btn-change-mode`、`finish-overlay`、`finish-grid`、`wrong-keys`、`btn-again`、`btn-finish-close` |
| 文章 | `view-article`、`article-setup`、`article-list`、`art-custom-text`、`btn-art-custom`、`article-area`、`art-title`、`art-hint`、`art-font`、`art-bold`、`btn-art-list-side`、`art-speed`、`art-progress`、`art-text`、`art-live`、`btn-art-restart`、`btn-art-exit`、`art-keyboard`、`art-finish`、`art-finish-title`、`art-finish-grid`、`art-wrong-keys`、`btn-art-again`、`btn-art-list` |
| 统计 | `view-stats`、`stats-cards`、`stats-daily`、`stats-keys` |
| 设置弹窗 | `modal`、`set-hint`、`set-sound`、`set-theme`、`set-fs`、`set-font`、`set-bold`、`btn-clear-data`、`btn-modal-close` |

## 11. 关键算法细节备忘

- **拼音提示**：`settings.hint` 为真时每个字格 `.py` 显示完整拼音，已敲码用 `<em class="hit">` 绿色加粗；为假时 `.art-text` 加 `nohint` 类隐藏 `.py`（连错 3 次临时 reveal 除外）。
- **准确率/速度**：准确率 = `correct/(correct+wrong)`；速度 = `itemsDone / (elapsed/60000)`。
- **完成判定**：普通文章 `countDone(flat) >= A.total` 或 `j >= flat.length`；好词好句用窗口内判据（见 7.7）。
- **退格边界**：不能退回 `sessionStartIdx` 之前（断点续练时保护上次进度）。
- **统计合并**：`mergeRoundIntoStats(rs, elapsedMs)`（app.js:757）累加 totalCorrect/totalWrong/totalItems/totalTimeMs/bestCombo，并把 wrongKeys 合并进 `byKey`，按天写 `days[todayStr()]`。
- **迷你键盘**：`renderArtKeyboard()`（app.js:465）三排键帽（qwerty + ;），复用 `currentScheme.displayMap`；点击调 `showKeyDetail`；实体键经 `ArticlePractice.onKey` → `flashArtKey(k)` 高亮 `.pressed`。

## 12. 常见修改场景操作指南

1. **加双拼方案**：只改 `app.js` 的 `SCHEMES`（6.1）。键盘图、方案选择器、文章编码自动生效。
2. **加内置文章**：只改 `articles.js`（§8）。列表、断点、已练徽标自动生效。
3. **改配色/主题**：优先改 `styles.css` 的 CSS 变量（9.1）；文章字色在 `.ac` 系列规则（9.3）。
4. **改文章字号/字体**：字体列表在 `index.html` 的 `#art-font` 与 `#set-font`（两个 select 要保持一致）+ `app.js` 的 `ARTICLE_FONTS`（822 行）。字号列表在 `#set-fs` + `app.js` 的 `FONT_SIZE_PX`（846 行）。
5. **改两行窗口高度/滚动位置**：`styles.css:359` 的 `height:calc(2*2.35em + 44px)` 与 `article.js:223 positionArtText()`。
6. **改速度曲线颜色/窗口长度**：`article.js:376 updateSpeedChart()`（最近 60 点、纵轴上限 200、红绿分段）。
7. **加设置项**：`index.html` 设置弹窗加控件 → `app.js` 的 `settings` 默认值（232 行）、`openModal`/`closeModal`（867/876）读写 → 需要的模块在 change 事件里响应。
8. **加文章页控件**：`index.html` 左栏 `#art-ctrls` → `article.js` buildSession 同步值 + 事件绑定（7.10）。
9. **改 localStorage 结构**：先看 §5 键表；注意向后兼容（旧数据可能缺字段，用 `lsGet(key, fallback)` + 字段兜底）。

## 13. 测试方法

### 13.1 语法检查

```bash
node --check app.js && node --check article.js && node --check articles.js && node --check version.js
```

### 13.2 无头 Chrome 冒烟/布局验证（可复用）

```powershell
# 1) 复制 index.html 为 _test_layout.html，在 </body> 前注入：
#    <pre id="report">pending</pre><script src="_test_inject.js"></script>
#    （app.js 顶层需要完整 index.html 的 DOM，不要用最小化测试页，否则报
#      top:Cannot read properties of undefined (reading 'onEscape')）

# 2) pwsh 运行（直接 & chrome.exe 捕获 stdout 为空；WSL 直接执行 chrome.exe 报
#    'cannot execute binary file: Exec format error'，必须走 Start-Process）：
Start-Process -FilePath 'C:\Program Files\Google\Chrome\Application\chrome.exe' `
  -ArgumentList @('--headless=new','--disable-gpu','--window-size=1440,900',`
  '--virtual-time-budget=6000','--dump-dom','file:///E:/.../_test_layout.html') `
  -Wait -PassThru -RedirectStandardOutput '_layout_out.html' -RedirectStandardError '_layout_err.txt'

# 3) 用 regex 提取 <pre id="report">([\s\S]*?)</pre> 里的 JSON 结果。
```

- 跨运行持久化（测 localStorage 恢复）：加 `--user-data-dir=<同一个目录>`。
- `--virtual-time-budget` 下 `requestAnimationFrame` 回调 ts 是虚拟时钟且帧不连续：要验证滚动动画最终位置，需 monkey-patch `requestAnimationFrame` 用 `setTimeout` 转发并让时间戳递增（或用 `setTimeout` 步进动画）。
- 测好词好句 fetch：在 _test_inject.js 里 mock `window.fetch`（`()=>Promise.reject(...)` 走内置回退，或自定义句子池）。

### 13.3 发布前手动清单

- [ ] 本地 file:// 打开：顶栏徽标显示「测试版」；文章/好词好句/退格/拼音提示/设置各功能过一遍。
- [ ] 四套方案编码抽查（含零声母、ü、分号键 ing）。
- [ ] `node --check` 全过。
- [ ] 按 §15 合并发布后，Pages 上确认顶栏徽标是正式版。

## 14. 已知坑与教训（改动前必读）

1. **`.ac` 字格回流**：`.py` 槽必须是固定宽（2.5rem），否则当前字拼音从长变短/再变长会导致字格忽宽忽窄、字不在原位。
2. **深色主题 + 护眼纸**：文章正文区在深色主题下也要保持浅纸色，`[data-theme="dark"] .art-text` 有专门覆盖；改主题时要一并检查当前字颜色（黑字）在两种主题下都可见。
3. **好词好句进度**：窗口删除旧句后，进度条显示必须用 `goodDoneBase + A.total`，否则进度会回退；改删除逻辑时同步维护 `goodWinStart/goodRemovedChars/goodDoneBase`。
4. **断点续练边界**：`sessionStartIdx` 保护上次进度，退格不能越过它。
5. **article.js 依赖 app.js 全局**：`$`、`settings`、`codeFor`、`mergeRoundIntoStats` 等都不是自带的，单独加载 article.js 会报错；测试桩也要模拟这些。
6. **脚本顺序**：version.js 必须在 app.js 之前；article.js 必须在 app.js 之后。
7. **发布合并后 version.js**：dev 的 `channel:"dev"` 会带进 main，发布前必须改回 `"release"`（§15）。
8. **本文件 dev-only**：合并到 main 时必须删除本文件（§15），否则它会跑到正式分支。

## 15. 发布检查清单（dev → main）

```bash
# 在 dev 上完成开发与测试
git add -A && git commit -m "描述改动"

# 合并到 main
git checkout main
git merge dev

# ① 删除 dev-only 文档
git rm 开发文档.md

# ② version.js 改回正式版（channel:"release"，顺手 bump 版本号）
#    编辑 version.js 后：
git add version.js
git commit -m "发布 vX.Y.Z"

# ③ 推送与打 tag
git push origin main
git tag vX.Y.Z && git push origin vX.Y.Z

# ④ 回到 dev 继续开发
git checkout dev
```

> 说明：dev 始终保留 `开发文档.md`，所以每次合并后都要在 main 上删除一次；
> 若嫌麻烦，可把删除动作固化进发布脚本。
